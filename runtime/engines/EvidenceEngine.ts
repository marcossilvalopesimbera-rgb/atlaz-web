import { EvidenceModel } from "../artifacts/EvidenceModel";

export default class EvidenceEngine {
  constructor() {}

  public build(): EvidenceModel {
    throw new Error("TODO: implement EvidenceEngine.build");
  }
}